plot(pamk_results[[5]]$pamobject)
plot(pamk_results[[12]]$pamobject)
plot(pamk_results[[5]]$pamobject)
plot(pamk_results[[40]]$pamobject)
?fviz_cluster
fviz_cluster(pamk_results[[40]]$pamobject)
?pamk
?pam.object
# pam with dissimilarity matrix
run_pam <- function(dist) {
map(
CLUSTERS,
~ structure(
pam(dist, k = .x),
dataset = attributes(dist)$dataset,
algorithm = "pam",
metric = attributes(dist)$metric,
centers = .x,
keep.diss = TRUE
))
}
pam_results <- map(metrics, run_pam) %>%
unlist(recursive = FALSE)
# fpc::pamk, which can use average silhouette width OR Calinski-Harabasz index, and the Duda-Hart test to determine whether more than one cluster is necessary.
run_pamk <- function(dist) {
structure(
pamk(dist, krange = CLUSTERS, criterion = "ch", alpha = 0.001),
dataset = names(object)[attributes(dist)$dataset],
algorithm = "pamk",
metric = attributes(dist)$metric
)
}
pamk_results <- map(metrics, run_pamk)
pamk_results %<>% discard(~ .x$nc == 1)
# !!! The following variable shows that the Caesar data, in no cases, justified more than one cluster. Not with Gorman, and not with the new custom variables. !!!
pamk_datasets <- unique(map_chr(pamk_results, ~ attributes(.x)$dataset))
# Find out what the average cluster number for each dataset is.
df <- map_df(pamk_results, ~ tibble(dataset = attributes(.x)$dataset, nc = .x$nc, metric = attributes(.x)$metric))
df |>
group_by(dataset) |>
summarize(median_nc = median(nc))
# pam with dissimilarity matrix
run_pam <- function(dist) {
map(
CLUSTERS,
~ structure(
pam(dist, k = .x),
dataset = attributes(dist)$dataset,
algorithm = "pam",
metric = attributes(dist)$metric,
centers = .x,
keep.data = TRUE
))
}
pam_results <- map(metrics, run_pam) %>%
unlist(recursive = FALSE)
# fpc::pamk, which can use average silhouette width OR Calinski-Harabasz index, and the Duda-Hart test to determine whether more than one cluster is necessary.
run_pamk <- function(dist) {
structure(
pamk(dist, krange = CLUSTERS, criterion = "ch", alpha = 0.001),
dataset = names(object)[attributes(dist)$dataset],
algorithm = "pamk",
metric = attributes(dist)$metric
)
}
pamk_results <- map(metrics, run_pamk)
pamk_results %<>% discard(~ .x$nc == 1)
# !!! The following variable shows that the Caesar data, in no cases, justified more than one cluster. Not with Gorman, and not with the new custom variables. !!!
pamk_datasets <- unique(map_chr(pamk_results, ~ attributes(.x)$dataset))
# Find out what the average cluster number for each dataset is.
df <- map_df(pamk_results, ~ tibble(dataset = attributes(.x)$dataset, nc = .x$nc, metric = attributes(.x)$metric))
df |>
group_by(dataset) |>
summarize(median_nc = median(nc))
# pam with dissimilarity matrix
run_pam <- function(dist) {
map(
CLUSTERS,
~ structure(
pam(dist, k = .x),
dataset = attributes(dist)$dataset,
algorithm = "pam",
metric = attributes(dist)$metric,
centers = .x
))
}
pam_results <- map(metrics, run_pam) %>%
unlist(recursive = FALSE)
# fpc::pamk, which can use average silhouette width OR Calinski-Harabasz index, and the Duda-Hart test to determine whether more than one cluster is necessary.
run_pamk <- function(dist) {
structure(
pamk(dist, krange = CLUSTERS, criterion = "ch", alpha = 0.001),
dataset = names(object)[attributes(dist)$dataset],
algorithm = "pamk",
metric = attributes(dist)$metric,
keep.data = TRUE
)
}
pamk_results <- map(metrics, run_pamk)
pamk_results %<>% discard(~ .x$nc == 1)
# !!! The following variable shows that the Caesar data, in no cases, justified more than one cluster. Not with Gorman, and not with the new custom variables. !!!
pamk_datasets <- unique(map_chr(pamk_results, ~ attributes(.x)$dataset))
# Find out what the average cluster number for each dataset is.
df <- map_df(pamk_results, ~ tibble(dataset = attributes(.x)$dataset, nc = .x$nc, metric = attributes(.x)$metric))
df |>
group_by(dataset) |>
summarize(median_nc = median(nc))
?pamk
# pam with dissimilarity matrix
run_pam <- function(dist) {
map(
CLUSTERS,
~ structure(
pam(dist, k = .x),
dataset = attributes(dist)$dataset,
algorithm = "pam",
metric = attributes(dist)$metric,
centers = .x
))
}
pam_results <- map(metrics, run_pam) %>%
unlist(recursive = FALSE)
# fpc::pamk, which can use average silhouette width OR Calinski-Harabasz index, and the Duda-Hart test to determine whether more than one cluster is necessary.
run_pamk <- function(dist) {
structure(
pamk(dist, krange = CLUSTERS, criterion = "ch", alpha = 0.001),
dataset = names(object)[attributes(dist)$dataset],
algorithm = "pamk",
metric = attributes(dist)$metric,
keep.data = TRUE,
keep.diss = TRUE
)
}
pamk_results <- map(metrics, run_pamk)
pamk_results %<>% discard(~ .x$nc == 1)
# !!! The following variable shows that the Caesar data, in no cases, justified more than one cluster. Not with Gorman, and not with the new custom variables. !!!
pamk_datasets <- unique(map_chr(pamk_results, ~ attributes(.x)$dataset))
# Find out what the average cluster number for each dataset is.
df <- map_df(pamk_results, ~ tibble(dataset = attributes(.x)$dataset, nc = .x$nc, metric = attributes(.x)$metric))
df |>
group_by(dataset) |>
summarize(median_nc = median(nc))
# pam with dissimilarity matrix
run_pam <- function(dist) {
map(
CLUSTERS,
~ structure(
pam(dist, k = .x),
dataset = attributes(dist)$dataset,
algorithm = "pam",
metric = attributes(dist)$metric,
centers = .x
))
}
pam_results <- map(metrics, run_pam) %>%
unlist(recursive = FALSE)
# fpc::pamk, which can use average silhouette width OR Calinski-Harabasz index, and the Duda-Hart test to determine whether more than one cluster is necessary.
run_pamk <- function(dist) {
structure(
pamk(dist, krange = CLUSTERS, criterion = "ch", alpha = 0.001),
dataset = names(object)[attributes(dist)$dataset],
algorithm = "pamk",
metric = attributes(dist)$metric,
keep.diss = TRUE
)
}
pamk_results <- map(metrics, run_pamk)
pamk_results %<>% discard(~ .x$nc == 1)
# !!! The following variable shows that the Caesar data, in no cases, justified more than one cluster. Not with Gorman, and not with the new custom variables. !!!
pamk_datasets <- unique(map_chr(pamk_results, ~ attributes(.x)$dataset))
# Find out what the average cluster number for each dataset is.
df <- map_df(pamk_results, ~ tibble(dataset = attributes(.x)$dataset, nc = .x$nc, metric = attributes(.x)$metric))
df |>
group_by(dataset) |>
summarize(median_nc = median(nc))
?pam.object
# pam with dissimilarity matrix
run_pam <- function(dist) {
map(
CLUSTERS,
~ structure(
pam(dist, k = .x),
dataset = attributes(dist)$dataset,
algorithm = "pam",
metric = attributes(dist)$metric,
centers = .x
))
}
pam_results <- map(metrics, run_pam) %>%
unlist(recursive = FALSE)
# fpc::pamk, which can use average silhouette width OR Calinski-Harabasz index, and the Duda-Hart test to determine whether more than one cluster is necessary.
run_pamk <- function(dist) {
structure(
pamk(dist, krange = CLUSTERS, criterion = "ch", alpha = 0.001, keep.diss = TRUE),
dataset = names(object)[attributes(dist)$dataset],
algorithm = "pamk",
metric = attributes(dist)$metric
)
}
pamk_results <- map(metrics, run_pamk)
pamk_results %<>% discard(~ .x$nc == 1)
# !!! The following variable shows that the Caesar data, in no cases, justified more than one cluster. Not with Gorman, and not with the new custom variables. !!!
pamk_datasets <- unique(map_chr(pamk_results, ~ attributes(.x)$dataset))
# Find out what the average cluster number for each dataset is.
df <- map_df(pamk_results, ~ tibble(dataset = attributes(.x)$dataset, nc = .x$nc, metric = attributes(.x)$metric))
df |>
group_by(dataset) |>
summarize(median_nc = median(nc))
# caesar_simplex, using Euclidean distance
v1 <- pamk_results[[30]]
fviz_cluster(v1$pamobject, v1$pamobject$diss)
fviz_cluster(v1$pamobject, v1$pamobject$diss)
# caesar_simplex, using Euclidean distance
v1 <- pamk_results[[30]]
fviz_cluster(v1$pamobject, v1$pamobject$diss)
# caesar_gorman, using Euclidean distance
v1 <- pamk_results[[32]]
fviz_cluster(v1$pamobject, v1$pamobject$diss)
# caesar_gorman, using Euclidean distance
v1 <- pamk_results[[32]]
fviz_cluster(v1$pamobject, v1$pamobject$diss)
fviz_cluster(v1$pamobject, v1$pamobject$diss) + geom_text_repel()
attributes(v1$pamobject)
attributes(v1$pamobject$diss)
fviz_cluster(v1$pamobject, v1$pamobject$diss) + geom_text_repel(label = v1$pamobject$diss$Labels)
fviz_cluster(v1$pamobject, v1$pamobject$diss) + geom_text_repel(label = attributes(v1$pamobject$diss)$Labels)
# caesar_simplex, using Euclidean distance
v1 <- pamk_results[[30]]
fviz_cluster(v1$pamobject, v1$pamobject$diss)
# caesar_gorman, using Euclidean distance
v1 <- pamk_results[[32]]
fviz_cluster(v1$pamobject, v1$pamobject$diss) + geom_text_repel(label = attributes(v1$pamobject$diss)$Labels)
plot(pamk_results[[32]]$pamobject)
plot(pamk_results[[30]]$pamobject)
# Cosine distance first
v1 <- pamk_results[[3]]
fviz_cluster(v1$pamobject, v1$pamobject$diss)
# Now Euclidean
v1 <- pamk_results[[31]]
fviz_cluster(v1$pamobject, v1$pamobject$diss)
# Cosine distance first
v1 <- pamk_results[[3]]
fviz_cluster(v1$pamobject, v1$pamobject$diss)
# Now Euclidean
v1 <- pamk_results[[31]]
fviz_cluster(v1$pamobject, v1$pamobject$diss)
?fviz_dist
# Cosine distance is clearly very difficult to visualize with PCA, since the clusters are being fit to the angle of the vector, which is in no way visible in the graph.
# As a result, we will visualize the hierarchical clustering instead
v1 <- pamk_results[[3]]
fviz_dist(v1$pamobject$diss)
?fviz_dend
# Cosine distance is clearly very difficult to visualize with PCA, since the clusters are being fit to the angle of the vector, which is in no way visible in the graph.
# As a result, we will visualize the hierarchical clustering instead
v1 <- pamk_results[[3]]
fviz_dend(v1$pamobject$diss |> hcut(hc_func = "agnes", hc_metric = "cosine"))
v1
# Cosine distance is clearly very difficult to visualize with PCA, since the clusters are being fit to the angle of the vector, which is in no way visible in the graph.
# As a result, we will visualize the hierarchical clustering instead
v1 <- pamk_results[[3]]
v1
?partition.object
v1$pamobject$silinfo
# Cosine distance is clearly very difficult to visualize with PCA, since the clusters are being fit to the angle of the vector, which is in no way visible in the graph.
# As a result, we will visualize the hierarchical clustering instead
v1 <- pamk_results[[3]]
fviz_dend(v1$pamobject$diss |> hcut(hc_func = "agnes", hc_metric = "cosine"))
# Visual inspection shows very little differentiation, and average silhouette width is very small at -0.0816. It seems that cosine is not a very valuable metric for this dataset.
# Now Euclidean
v1 <- pamk_results[[31]]
fviz_cluster(v1$pamobject, v1$pamobject$diss)
# cosine for simplex
v1 <- pamk_results[[1]]
fviz_cluster(v1$pamobject, v1$pamobject$diss)
# cosine for custom
v1 <- pamk_results[[5]]
fviz_cluster(v1$pamobject, v1$pamobject$diss)
# cosine for simplex
v1 <- pamk_results[[1]]
fviz_cluster(v1$pamobject, v1$pamobject$diss)
print(v1$pamobject$silinfo)
# cosine for custom
v1 <- pamk_results[[5]]
fviz_cluster(v1$pamobject, v1$pamobject$diss)
print(v1$pamobject$silinfo)
# Agglomerative clustering (use AGNES, put the agglomerative coefficient somewhere visible)
# If we add the linkage methods "flexible" or "gaverage", we would have to be familiar with the Lance-Williams formula and parameters, and I do not trust my expertise in that regard. See the documentation for the `agnes()` function.
run_agnes <- function(dist) {
structure(
agnes(dist, method = "ward"),
dataset = attributes(dist)$dataset,
algorithm = "agnes",
metric = attributes(dist)$metric,
linkage = "ward"
)
}
agnes_results <- map(metrics, run_agnes) %>%
unlist(recursive = FALSE)
# What is the average agglomerative coefficient for each linkage method? This will help decide which linkage method is best for pvclust down below.
ac_values <- list(linkage = character(), ac = double())
for (item in agnes_results) {
ac_values$linkage <- c(ac_values$linkage, attributes(item)$linkage)
ac_values$ac <- c(ac_values$ac, item$ac)
}
View(agnes_results)
# Agglomerative clustering (use AGNES, put the agglomerative coefficient somewhere visible)
# If we add the linkage methods "flexible" or "gaverage", we would have to be familiar with the Lance-Williams formula and parameters, and I do not trust my expertise in that regard. See the documentation for the `agnes()` function.
run_agnes <- function(dist) {
structure(
agnes(dist, method = "ward"),
dataset = attributes(dist)$dataset,
algorithm = "agnes",
metric = attributes(dist)$metric,
linkage = "ward"
)
}
agnes_results <- map(metrics, run_agnes) %>%
unlist(recursive = FALSE)
agnes_results <- map(metrics, run_agnes)
# What is the average agglomerative coefficient for each linkage method? This will help decide which linkage method is best for pvclust down below.
ac_values <- list(linkage = character(), ac = double())
for (item in agnes_results) {
ac_values$linkage <- c(ac_values$linkage, attributes(item)$linkage)
ac_values$ac <- c(ac_values$ac, item$ac)
}
ac_values <- as.data.frame(ac_values)
ac_values |> group_by(linkage) |> summarize(mean = mean(ac)) |> ggplot(aes(linkage, mean)) + geom_col()
ac_values
# Agglomerative clustering (use AGNES, put the agglomerative coefficient somewhere visible)
# If we add the linkage methods "flexible" or "gaverage", we would have to be familiar with the Lance-Williams formula and parameters, and I do not trust my expertise in that regard. See the documentation for the `agnes()` function.
run_agnes <- function(dist) {
structure(
agnes(dist, method = "ward"),
dataset = names(object)[attributes(dist)$dataset],
algorithm = "agnes",
metric = attributes(dist)$metric,
linkage = "ward"
)
}
agnes_results <- map(metrics, run_agnes)
# What is the average agglomerative coefficient for each linkage method? This will help decide which linkage method is best for pvclust down below.
ac_values <- list(linkage = character(), ac = double(), dataset = character())
for (item in agnes_results) {
ac_values$linkage <- c(ac_values$linkage, attributes(item)$linkage)
ac_values$ac <- c(ac_values$ac, item$ac)
ac_values$dataset <- c(ac_values$dataset, attributes(item)$dataset)
}
ac_values <- as.data.frame(ac_values)
View(ac_values)
# What is the average agglomerative coefficient for each linkage method? This will help decide which linkage method is best for pvclust down below.
ac_values <- list(linkage = character(), ac = double(), dataset = character(), metric = character())
for (item in agnes_results) {
ac_values$linkage <- c(ac_values$linkage, attributes(item)$linkage)
ac_values$ac <- c(ac_values$ac, item$ac)
ac_values$dataset <- c(ac_values$dataset, attributes(item)$dataset)
ac_values$metric <- c(ac_values$metric, attributes(item)$metric)
}
ac_values <- as.data.frame(ac_values)
View(caesar_custom_and_gorman)
View(gorman.vars)
verify_random_cell(gorman.vars, data)
library(conflicted)
library(factoextra)
#library(dplyr)
library(ggrepel)
library(tidyverse)
library(cluster)
library(dendextend)
library(combinat)
library(MASS)
library(multcomp)
library(ggtext)
library(printr)
library(magrittr)
library(logrittr)
library(data.table)
library(collapse)
library(logger)
library(dendextend)
library(ggdendro)
library(fclust)
library(lsa)
library(fpc)
library(pvclust)
library(cowplot)
library(gt)
conflict_prefer("filter", "dplyr")
conflicted::conflicts_prefer(combinat::combn)
conflicted::conflicts_prefer(dplyr::select)
#For debugging purposes, here is a vector to save the size of the lower quartile that is discarded when variables are counted. This number is the minimum number of times per 1000 words a variable can occur in that section without getting discarded
lower_quartiles <- vector(mode = "double")
log_threshold(level = DEBUG)
source("functions.R")
# dims <- c(
#   "title",
#   "author",
#   "book",
#   "section",
#   "path",
#   "form",
#   "lemma",
#   "tag",
#   "Aspect",
#   "Mood",
#   "Number",
#   "Person",
#   "Tense",
#   "VerbForm",
#   "Voice",
#   "Case",
#   "PronType",
#   "Gender",
#   "Polarity",
#   "Degree",
#   "NumType",
#   "Deprel",
#   "parent_form",
#   "parent_lemma",
#   "parent_tag",
#   "parent_Aspect",
#   "parent_Mood",
#   "parent_Number",
#   "parent_Person",
#   "parent_Tense",
#   "parent_VerbForm",
#   "parent_Voice",
#   "parent_Case",
#   "parent_PronType",
#   "parent_Gender",
#   "parent_Polarity",
#   "parent_Degree",
#   "parent_NumType",
#   "parent_Deprel"
# )
data <- fread("./postagged/postagged-texts.csv", na.strings = "", header = TRUE, stringsAsFactors = TRUE)
data <- bind_rows(
data,
fread("./postagged/postagged-cicero.csv", na.strings = "", col.names = colnames(data), stringsAsFactors = TRUE)[-1,],
fread("./postagged/postagged-sallust.csv", na.strings = "", col.names = colnames(data), stringsAsFactors = TRUE)[-1,]
)
data %<>%
mutate(Person = as.factor(Person)) %>%
dplyr::select(-Voice)
data %<>% dplyr::select(-parent_Voice)
summary(data)
# Change some of the data types and rename certain values; details available in functions.R
data %<>% prepare_data()
summary(data)
verify_random_cell(gorman.vars, data)
View(data)
?write_csv
strsplit("stuff.things", "[.]")
get_count_by_title_features <- function(title, feature_char, source_data, pipe = TRUE) {
# Remove space from the title
title_selected <- gsub(pattern = " ", replacement = "", title)
# Split column name into a character vector
if (pipe)  vars_unsplit <- unlist(strsplit(feature_char, "[|]"))
else vars_unsplit <- unlist(strsplit(feature_char, "[.]"))
# Filter source_data to the title,
#   using partial matches with str_starts() from stringr package
filtered_data <- source_data |>
filter(str_starts(gsub(" ", "", title), title_selected))
# Pass character vector and source_data to
#   get_number_rows_with_feature_types()
count_from_source_data <- get_number_rows_with_feature_types(
vars_unsplit,
source_data = filtered_data
)
# Normalize the value per 1000 words
(count_from_source_data / nrow(filtered_data)) * 1000
}
# Set pipe to FALSE if each combined variable is separated by periods instead
verify_random_cell <- function(all_vars, source_data, pipe = TRUE) {
# For logging purposes
fn_name <- get_logger_meta_variables(log_level = INFO)$fn
# Select a random cell from all_vars
rand_row <- slice_sample(all_vars)
column_number <- sample(2:ncol(rand_row), size = 1) # column number of the
# targeted cell, excluding
# titles
cell <- rand_row[, column_number]
# Get its title and column name
title <- rand_row$title
colname <- names(all_vars)[column_number]
log_info("{fn_name}: Selecting all_vars[title == {title}, '{colname}']")
log_info("{fn_name}: Value of cell is {cell}")
count_from_source_data <- get_count_by_title_features(
title = title,
feature_char = colname,
source_data = source_data,
pipe
)
log_info("{fn_name}: The count retrieved from the source data is {count_from_source_data}")
log_info("{fn_name}: count == cell: {signif(count_from_source_data, digits = 2) == signif(cell, digits = 2)}")
(signif(count_from_source_data, digits = 2) == signif(cell, digits = 2))[[1]]
}
verify_random_cell(gorman.vars, data, FALSE)
verify_random_cell(simplex, data, FALSE)
verify_random_cell(simplex_vars_only, data, FALSE)
verify_random_cell(simplex_vars_only, data, FALSE)
pvclust_results <- c(
# A map function for each distance metric with different linkage methods
imap(
object,
\(x, y) structure(pvclust(x, "ward.D2", "euclidean"), dataset = y, method = "ward.D2", metric = "euclidean")
)
)
?nselectboot
sectioned <- list(
gorman = sectioned.gorman.vars,
custom = custom_vars_section
)
