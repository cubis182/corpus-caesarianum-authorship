#
#character select : A character value which is the section/book we're using in this function
count_combinations_per_section <- function(combinations, source_data, title_str) {
#browser()
norm_val <- 1000 / nrow(source_data)
combinations |>
dplyr::rowwise() %>=%
summarize(
n = get_number_rows_with_feature_types(c_across(everything()), source_data)
) %>%
bind_cols(combinations, .) |>
mutate(n = n * norm_val) |>
mutate(title = title_str)
}
#library(dtplyr)
run_combos <- function(source_data, title_str) {
t1 <- proc.time()
#browser()
all_vars <- count_var_combos(combinations, source_data) |> as_tibble(.name_repair = "universal_quiet")
names(all_vars)[1] <- "pasted"
#browser()
#Normalize frequencies by occurrence per 1000 words
norm_val <- 1000 / nrow(source_data)
all_vars <- all_vars |>
mutate(n = n * norm_val)
all_vars <- all_vars |>
mutate(title = title_str) #The titles should all be the same...
print(proc.time() - t1)
return(all_vars)
}
#mode: whether to divide the data into commentaries, books, or chapters
#If getting the variables from an existing data frame, make sure they're
# in the combinations variable and use_preset is set to TRUE
get_vars <- function(mode = "book", source, presupplied_variables = NULL) {
if (mode == "book") {
#browser()
source_data <- source |>
rowwise() |>
mutate(title = unlist(strsplit(title, "_"))[1:2] |> paste(collapse="_"))
}
else if (mode == "commentary") {
source_data <- source |>
rowwise() |>
mutate(title = unlist(strsplit(title, "_"))[1])
}
else if (mode == "chapter") {
source_data <- source
}
else {
return(NULL)
}
#browser()
if (is.null(presupplied_variables)) {
all_vars <- source_data |>
group_by(title) |>
#lazy_dt() |>
group_map(~ run_combos(.x, .y)) |>
bind_rows() |>
mutate(title = unlist(title))
} else {
#browser()
all_vars <- source_data |>
#For debugging:
#slice_sample(n = 100) |>
rowwise() |>
mutate(title = unlist(strsplit(title, "_"))[1:2] |> paste(collapse="_")) |>
group_by(title) %>=%
group_map(~ count_combinations_per_section(presupplied_variables, source_data = .x, title_str = .y)) |>
bind_rows() |>
mutate(title = unlist(title)) |>
unite(col = "pasted", 1:5, sep = "|", na.rm = TRUE)
all_vars %<>%
transpose_all_vars() |>
select_top_variables(n = 1000)
}
return(all_vars)
}
#Restore the full data
# data <- data_backup
# #
# remove(data_backup)
# remove(data_sample)
if (!("future.apply" %in% (.packages()))) library(future.apply)
if (!("profvis" %in% (.packages()))) library(profvis)
if (!("dtplyr" %in% (.packages()))) library(dtplyr)
all_vars <- get_vars("chapter", data, presupplied_variables=NULL)
#Restore the full data
# data <- data_backup
# #
# remove(data_backup)
# remove(data_sample)
if (!("future.apply" %in% (.packages()))) library(future.apply)
if (!("profvis" %in% (.packages()))) library(profvis)
if (!("dtplyr" %in% (.packages()))) library(dtplyr)
all_vars <- get_vars("chapter", data, presupplied_variables=combinations)
#Restore the full data
# data <- data_backup
# #
# remove(data_backup)
# remove(data_sample)
if (!("future.apply" %in% (.packages()))) library(future.apply)
if (!("profvis" %in% (.packages()))) library(profvis)
if (!("dtplyr" %in% (.packages()))) library(dtplyr)
all_vars <- get_vars("book", data, presupplied_variables=combinations)
?slice
slice(data, 1:30, by = title)
slice(data, 1:30, .by = title)
data$title
length(unique(data$title))
1598 * 30
#Restore the full data
# data <- data_backup
# #
# remove(data_backup)
# remove(data_sample)
if (!("future.apply" %in% (.packages()))) library(future.apply)
if (!("profvis" %in% (.packages()))) library(profvis)
if (!("dtplyr" %in% (.packages()))) library(dtplyr)
system.time({
all_vars <- get_vars("book", data |> slice(1:10, .by = path), presupplied_variables=combinations)
})
warnings()
traceback()
#
#character select : A character value which is the section/book we're using in this function
count_combinations_per_section <- function(combinations, source_data, title_str) {
#browser()
norm_val <- 1000 / nrow(source_data)
combinations |>
dplyr::rowwise() %>%
summarize(
n = get_number_rows_with_feature_types(c_across(everything()), source_data)
) %>%
bind_cols(combinations, .) |>
mutate(n = n * norm_val) |>
mutate(title = title_str)
}
#library(dtplyr)
run_combos <- function(source_data, title_str) {
t1 <- proc.time()
#browser()
all_vars <- count_var_combos(combinations, source_data) |> as_tibble(.name_repair = "universal_quiet")
names(all_vars)[1] <- "pasted"
#browser()
#Normalize frequencies by occurrence per 1000 words
norm_val <- 1000 / nrow(source_data)
all_vars <- all_vars |>
mutate(n = n * norm_val)
all_vars <- all_vars |>
mutate(title = title_str) #The titles should all be the same...
print(proc.time() - t1)
return(all_vars)
}
#mode: whether to divide the data into commentaries, books, or chapters
#If getting the variables from an existing data frame, make sure they're
# in the combinations variable and use_preset is set to TRUE
get_vars <- function(mode = "book", source, presupplied_variables = NULL) {
if (mode == "book") {
#browser()
source_data <- source |>
rowwise() |>
mutate(title = unlist(strsplit(title, "_"))[1:2] |> paste(collapse="_"))
}
else if (mode == "commentary") {
source_data <- source |>
rowwise() |>
mutate(title = unlist(strsplit(title, "_"))[1])
}
else if (mode == "chapter") {
source_data <- source
}
else {
return(NULL)
}
#browser()
if (is.null(presupplied_variables)) {
all_vars <- source_data |>
group_by(title) |>
#lazy_dt() |>
group_map(~ run_combos(.x, .y)) |>
bind_rows() |>
mutate(title = unlist(title))
} else {
#browser()
all_vars <- source_data |>
#For debugging:
#slice_sample(n = 100) |>
rowwise() |>
mutate(title = unlist(strsplit(title, "_"))[1:2] |> paste(collapse="_")) |>
group_by(title) %>%
group_map(~ count_combinations_per_section(presupplied_variables, source_data = .x, title_str = .y)) |>
bind_rows() |>
mutate(title = unlist(title)) |>
unite(col = "pasted", 1:5, sep = "|", na.rm = TRUE)
all_vars %<>%
transpose_all_vars() |>
select_top_variables(n = 1000)
}
return(all_vars)
}
#Restore the full data
# data <- data_backup
# #
# remove(data_backup)
# remove(data_sample)
if (!("future.apply" %in% (.packages()))) library(future.apply)
if (!("profvis" %in% (.packages()))) library(profvis)
if (!("dtplyr" %in% (.packages()))) library(dtplyr)
system.time({
all_vars <- get_vars("book", data |> slice(1:10, .by = path), presupplied_variables=combinations)
})
#all_vars <- all_vars |> mutate(title = unlist(title)
#First, like Gorman, remove ones appearing in less than 20% of the corpus. For section-by-section, that's 134 / 5 = 27
#Recommended operation on the output of get_vars()
transpose_all_vars <- function(all_vars) {
all_vars <- all_vars |> mutate(title = as.factor(title))
all_vars <- pivot_wider(
all_vars,
names_from = pasted,
values_from = n,
values_fill = 0)
return(all_vars)
}
#Reduces output of transpose_all_vars to top n variables
select_top_variables <- function(all_vars, n) {
top_cols <- all_vars |>
dplyr::select(-title) |>
summarize(across(everything(), sum, na.rm = TRUE)) |>
pivot_longer(cols = everything(), names_to = "col_name", values_to = "total") |>
arrange(desc(total)) |>
slice_head(n = n) |>
pull(col_name)
all_vars_top_thousand <- all_vars |>
dplyr::select(any_of(top_cols)) |>
mutate(title = all_vars$title) |>
relocate(title, .before = 1)
}
# title_temp <- all_vars$title
# all_vars <- all_vars |> dplyr::select(-title) |> dplyr::select(where(~ sum(.x, na.rm = TRUE) > length(.x) * 30)) |> mutate(title = title_temp)
#
# all_vars <- all_vars |> relocate(title, .before = everything())
#
# remove(title_temp)
#Restore the full data
# data <- data_backup
# #
# remove(data_backup)
# remove(data_sample)
if (!("future.apply" %in% (.packages()))) library(future.apply)
if (!("profvis" %in% (.packages()))) library(profvis)
if (!("dtplyr" %in% (.packages()))) library(dtplyr)
system.time({
all_vars <- get_vars("book", data |> slice(1:10, .by = path), presupplied_variables=combinations)
})
system.time({
all_vars <- get_vars("book", data |> slice(1:10, .by = path), presupplied_variables=NULL)
})
#Restore the full data
# data <- data_backup
# #
# remove(data_backup)
# remove(data_sample)
if (!("future.apply" %in% (.packages()))) library(future.apply)
if (!("profvis" %in% (.packages()))) library(profvis)
if (!("dtplyr" %in% (.packages()))) library(dtplyr)
system.time({
all_vars <- get_vars("book", data |> slice(1:10, .by = path), presupplied_variables=combinations)
print("Finished task 1")
})
system.time({
all_vars <- get_vars("book", data |> slice(1:10, .by = path), presupplied_variables=NULL)
print("Finished task 2")
})
#
#character select : A character value which is the section/book we're using in this function
count_combinations_per_section <- function(combinations, source_data, title_str) {
#browser()
t1 <- proc.time()
norm_val <- 1000 / nrow(source_data)
return_value <- combinations |>
dplyr::rowwise() %>%
summarize(
n = get_number_rows_with_feature_types(c_across(everything()), source_data)
) %>%
bind_cols(combinations, .) |>
mutate(n = n * norm_val) |>
mutate(title = title_str)
print(proc.time() - t1)
return_value
}
#Restore the full data
# data <- data_backup
# #
# remove(data_backup)
# remove(data_sample)
if (!("future.apply" %in% (.packages()))) library(future.apply)
if (!("profvis" %in% (.packages()))) library(profvis)
if (!("dtplyr" %in% (.packages()))) library(dtplyr)
system.time({
all_vars <- get_vars("book", data |> slice(1:10, .by = path), presupplied_variables=combinations)
print("Finished task 1")
})
system.time({
all_vars <- get_vars("book", data |> slice(1:10, .by = path), presupplied_variables=NULL)
print("Finished task 2")
})
#Restore the full data
# data <- data_backup
# #
# remove(data_backup)
# remove(data_sample)
if (!("future.apply" %in% (.packages()))) library(future.apply)
if (!("profvis" %in% (.packages()))) library(profvis)
if (!("dtplyr" %in% (.packages()))) library(dtplyr)
all_vars <- get_vars("book", data |> slice(1:10, .by = path), presupplied_variables=combinations)
View(all_vars)
data |> slice(1:10, .by = path)
combinations
View(combinations)
View(all_vars)
data |> slice(1:10, .by = path)
data |> rowwise() |>
mutate(title = unlist(strsplit(title, "_"))[1:2] |> paste(collapse="_")) |>
group_by(title)
?slice
#Restore the full data
# data <- data_backup
# #
# remove(data_backup)
# remove(data_sample)
if (!("future.apply" %in% (.packages()))) library(future.apply)
if (!("profvis" %in% (.packages()))) library(profvis)
if (!("dtplyr" %in% (.packages()))) library(dtplyr)
#Takes roughly 3 minutes for books,
all_vars <- get_vars("book", data |> slice_sample(1:100), presupplied_variables=combinations)
#Restore the full data
# data <- data_backup
# #
# remove(data_backup)
# remove(data_sample)
if (!("future.apply" %in% (.packages()))) library(future.apply)
if (!("profvis" %in% (.packages()))) library(profvis)
if (!("dtplyr" %in% (.packages()))) library(dtplyr)
#Takes roughly 3 minutes for books,
all_vars <- get_vars("book", data |> slice_sample(n = 1:100), presupplied_variables=combinations)
#Restore the full data
# data <- data_backup
# #
# remove(data_backup)
# remove(data_sample)
if (!("future.apply" %in% (.packages()))) library(future.apply)
if (!("profvis" %in% (.packages()))) library(profvis)
if (!("dtplyr" %in% (.packages()))) library(dtplyr)
#Takes roughly 3 minutes for books,
all_vars <- get_vars("book", data |> slice_sample(n = 100), presupplied_variables=combinations)
View(all_vars)
has_all_feature_types <- function(row_from_source_data, character_vector_of_feature_values) {
# Unnecessary when using apply() around this function
# row_from_source_data %<>% mutate(across(everything(), as.character))
# browser()
# Only return true for this row if ALL characters in the vector on the left are in the row on the right
browser()
all(as.character(character_vector_of_feature_values) %in% row_from_source_data)
}
#Restore the full data
# data <- data_backup
# #
# remove(data_backup)
# remove(data_sample)
if (!("future.apply" %in% (.packages()))) library(future.apply)
if (!("profvis" %in% (.packages()))) library(profvis)
if (!("dtplyr" %in% (.packages()))) library(dtplyr)
#Takes roughly 3 minutes for books,
all_vars <- get_vars("book", data |> slice_sample(n = 100), presupplied_variables=combinations)
force(character_vector_of_feature_values)
all(as.character(character_vector_of_feature_values) %in% row_from_source_data)
#Requires a row from a tibble that only includes valid column names and no numerics / other data types
get_var_combos <- function(var_combn, source_data) {
#browser()
#This is used to select the desired columns
selection_char <- as.character(var_combn) |> na.omit()
var_combos <-
source_data |>
#Now remove NAs, since the combinations vector might include them
dplyr::select(all_of(as.character(var_combn) |> na.omit())) %>%
#na.omit might be faster than my custom function, and it performs the same task.
#I've tested to ensure the number of rows is the same no matter which function I use, and even when multiple columns are selected
na.omit() |>
#Added next line to avoid expensive string comparisons
#dplyr::select(where(all(~ !is.na(.x)))) |>
#Paste the whole row together; I use a pipe because it's unused elsewhere
unite("pasted", everything(), sep = "|")
#Tried to replace the unite() function just above with the below code, but it was significantly slower
#apply(MARGIN = 1, FUN = paste0, collapse = "|")
#filter(str_detect(pasted, pattern = "NA", negate = TRUE)) #Remove NAs
# browser()
if (length(var_combos$pasted) > 0) {
#final.table <- table(var_combos) #|> as_tibble()
#browser()
#Return only the ones above an arbitrary threshold.
return(var_combos$pasted) #|> filter(n > 30)) #initially not 30, but nrow(source_data / 5)
}
else {return(NULL)}
}
count_var_combos <- function(var_combn, source_data) {
list_combn <- apply(var_combn, MARGIN = 1, get_var_combos, source_data = source_data) |> unlist() |> table() #cbind()
#browser()
#Initially removed low-frequency items, but that resulted in NA values in the final data.
return(list_combn) #subset(list_combn, list_combn > nrow(source_data) / 10)) # |> as_tibble() |> filter(V1 > 30))
}
#count_var_combos(two_combinations, data)
has_all_feature_types <- function(row_from_source_data, character_vector_of_feature_values) {
# Unnecessary when using apply() around this function
# row_from_source_data %<>% mutate(across(everything(), as.character))
# browser()
# Only return true for this row if ALL characters in the vector on the left are in the row on the right
all(as.character(character_vector_of_feature_values) %in% row_from_source_data)
}
get_number_rows_with_feature_types <- function(row_of_feature_values, source_data) {
#browser()
count_of_feature_values <- sum(
apply(X = source_data, MARGIN = 1, FUN = has_all_feature_types, character_vector_of_feature_values = row_of_feature_values)
)
count_of_feature_values
}
#
#character select : A character value which is the section/book we're using in this function
count_combinations_per_section <- function(combinations, source_data, title_str) {
#browser()
t1 <- proc.time()
norm_val <- 1000 / nrow(source_data)
return_value <- combinations |>
dplyr::rowwise() %>%
summarize(
n = get_number_rows_with_feature_types(c_across(everything()), source_data)
) %>%
bind_cols(combinations, .) |>
mutate(n = n * norm_val) |>
mutate(title = title_str)
print(proc.time() - t1)
return_value
}
#library(dtplyr)
run_combos <- function(source_data, title_str) {
t1 <- proc.time()
#browser()
all_vars <- count_var_combos(combinations, source_data) |> as_tibble(.name_repair = "universal_quiet")
names(all_vars)[1] <- "pasted"
#browser()
#Normalize frequencies by occurrence per 1000 words
norm_val <- 1000 / nrow(source_data)
all_vars <- all_vars |>
mutate(n = n * norm_val)
all_vars <- all_vars |>
mutate(title = title_str) #The titles should all be the same...
print(proc.time() - t1)
return(all_vars)
}
#mode: whether to divide the data into commentaries, books, or chapters
#If getting the variables from an existing data frame, make sure they're
# in the combinations variable and use_preset is set to TRUE
get_vars <- function(mode = "book", source, presupplied_variables = NULL) {
if (mode == "book") {
#browser()
source_data <- source |>
rowwise() |>
mutate(title = unlist(strsplit(title, "_"))[1:2] |> paste(collapse="_"))
}
else if (mode == "commentary") {
source_data <- source |>
rowwise() |>
mutate(title = unlist(strsplit(title, "_"))[1])
}
else if (mode == "chapter") {
source_data <- source
}
else {
return(NULL)
}
#browser()
if (is.null(presupplied_variables)) {
all_vars <- source_data |>
group_by(title) |>
#lazy_dt() |>
group_map(~ run_combos(.x, .y)) |>
bind_rows() |>
mutate(title = unlist(title))
} else {
#browser()
all_vars <- source_data |>
#For debugging:
#slice_sample(n = 100) |>
rowwise() |>
mutate(title = unlist(strsplit(title, "_"))[1:2] |> paste(collapse="_")) |>
group_by(title) %>%
group_map(~ count_combinations_per_section(presupplied_variables, source_data = .x, title_str = .y)) |>
bind_rows() |>
mutate(title = unlist(title)) |>
unite(col = "pasted", 1:5, sep = "|", na.rm = TRUE)
all_vars %<>%
transpose_all_vars() |>
select_top_variables(n = 1000)
}
return(all_vars)
}
all_vars <- get_vars("book", data |> slice(1:10, .by = path), presupplied_variables=NULL)
View(all_vars)
all_vars %<>%
transpose_all_vars() |>
select_top_variables(n = 1000)
View(all_vars)
View(data)
