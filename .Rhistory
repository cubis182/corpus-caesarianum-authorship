aes(x = x_axis, y = y_axis, label = value),
color = "black",
size.unit = "pt",
size = 6,
inherit.aes = FALSE)
graph <- fviz_dist(gorman_dist, show_labels = TRUE)
graph <- graph + geom_text(
data = d_long,
aes(x = x_axis, y = y_axis, label = value),
color = "black",
size.unit = "pt",
size = 6,
inherit.aes = FALSE)
graph + scale_fill_gradient2(low = "blue", high = "red", midpoint = mean(unlist(scores.correlation.per.text)))
graph <- fviz_dist(gorman_dist, show_labels = TRUE)
graph <- graph + geom_text(
data = d_long,
aes(x = x_axis, y = y_axis, label = value),
color = "black",
size.unit = "pt",
size = 5,
inherit.aes = FALSE)
graph + scale_fill_gradient2(low = "blue", high = "red", midpoint = mean(unlist(scores.correlation.per.text)))
ggsave("Images/dissimilarity_matrix_between_books.png", dpi = "print")
graph + scale_fill_gradient2(low = "blue", high = "red", midpoint = 0.1)
ggsave("Images/dissimilarity_matrix_between_books.png", dpi = "print")
graph + scale_fill_gradient2(low = "red", high = "blue", midpoint = 0.1)
ggsave("Images/dissimilarity_matrix_between_books.png", dpi = "print")
View(sectioned.gorman.vars)
?str_starts
library(stringr)
cicero_works <- "(philippics|senectute|amicitia)"
sectioned.gorman.vars |> filter(str_starts(title, cicero_works))
sectioned.gorman.vars |> filter(str_starts(title, cicero_works)) |> pull(title) |> unique()
sectioned.gorman.vars <- bind_rows(
filter(sectioned.gorman.vars, str_starts(title, cicero_works)),
filter(sectioned.gorman.vars, !str_starts(title, cicero_works))
)
View(sectioned.gorman.vars)
gorman_dist <- get_dist(gorman.vars |> dplyr::select(-title), method = "spearman")
gorman.vars <- bind_rows(
View(gorman.vars)
akdjlfs
View(gorman.vars)
gorman.vars <- bind_rows(
asdfad
)
gorman.vars <- bind_rows(
filter(gorman.vars, str_starts(title, cicero_works)),
filter(gorman.vars, !str_starts(title, cicero_works))
)
gorman_dist <- get_dist(gorman.vars |> dplyr::select(-title), method = "spearman")
mat <- as.matrix(gorman_dist)
d_long <- expand.grid(Var1 = rownames(mat), Var2 = colnames(mat))
d_long$value <- as.vector(mat)
d_long$value <- round(d_long$value, digits = 2)
d_long$x_axis <- as.integer(d_long$Var1)
d_long$y_axis <- as.integer(d_long$Var2)
d_long <- as.data.frame(d_long)
graph <- fviz_dist(gorman_dist, show_labels = TRUE)
graph <- graph + geom_text(
data = d_long,
aes(x = x_axis, y = y_axis, label = value),
color = "black",
size.unit = "pt",
size = 5,
inherit.aes = FALSE)
graph + scale_fill_gradient2(low = "red", high = "blue", midpoint = 0.1)
ggsave("Images/dissimilarity_matrix_between_books.png", dpi = "print")
gorman_dist
d_long
graph <- fviz_dist(gorman_dist, show_labels = TRUE)
graph
scores.correlation.per.text <- tapply(sectioned.gorman.vars, INDEX = sectioned.gorman.vars$book, FUN = within.book.similarity, rows = 20, type_of_similarity = "spearman")
library(logger)
#Get a vector of all the titles2
titles <- unique(sectioned.gorman.vars$book)
#Get the spearman correlation coefficients, add them to a list, with the label being the title
scores.correlation.per.text <- tapply(sectioned.gorman.vars, INDEX = sectioned.gorman.vars$book, FUN = within.book.similarity, rows = 20, type_of_similarity = "spearman")
if (!'pspearman' %in% .packages()) library(pspearman)
if (!'Kendall' %in% .packages()) library(Kendall)
if (!'lsa' %in% .packages()) library(lsa)
retrieve_combination_similarity <- function(combo, section_vectors, type) {
vectors <-section_vectors |> filter(title %in% combo) |> dplyr::select(-title, -path, -book)
#browser()
vec_a <- vectors[1,] |> t() |> as.vector()
vec_b <- vectors[2,] |> t() |> as.vector()
#browser()
#If using Spearman, use approximation AS89 when the number of variables is under 1,290
if (type == "spearman") {
amount_jitter <- 0.001
vec_a %<>% jitter(amount = amount_jitter)
vec_b %<>% jitter(amount = amount_jitter)
rho_estimate <- spearman.test(vec_a, vec_b, alternative = "two.sided", approximation = "AS89")$estimate
#Note: we'll be comparing this with dissimilarity scores, which are actually the absolute value of (rho - 1)
return(abs(rho_estimate - 1.0))
} else if (type == "cosine" ) {
return(cosine(vec_a, vec_b))
} else if (type == "euclidean") {
return(dist(rbind(vec_a, vec_b)))
}
}
#Parameters:
#  character book: Name of commentary book. Valid values: "gallic _ 1", "gallic _ 2", etc.
#  tibble section_vectors: NEEDSDOC
# double prop: The proportion of sections to sample from to save on computation; should be 1 in the final article
within.book.similarity <- function(section_vectors, rows, type_of_similarity = "spearman") {
sub_section_vectors <- section_vectors[sample(1:nrow(section_vectors), rows),]
#browser()
log_info("Starting sub_section_vectors...")
t1 <- proc.time()
#Use the columns if this to select
#combn(sectioned.gorman.vars |> filter(path == "african ") |> pull(title), 2)
combos <- combn(sub_section_vectors$title, 2)
similarities <- apply(X = combos, MARGIN = 2, FUN = retrieve_combination_similarity, section_vectors = sub_section_vectors, type = type_of_similarity)
print("Finished one book in within.book.similarity")
print(proc.time() - t1)
return(similarities)
}
#Get a vector of all the titles2
titles <- unique(sectioned.gorman.vars$book)
#Get the spearman correlation coefficients, add them to a list, with the label being the title
scores.correlation.per.text <- tapply(sectioned.gorman.vars, INDEX = sectioned.gorman.vars$book, FUN = within.book.similarity, rows = 20, type_of_similarity = "spearman")
rownames(gorman.vars) <- gorman.vars$title
gorman_dist <- get_dist(gorman.vars |> dplyr::select(-title), method = "spearman")
names(scores.correlation.per.text) <- titles
rownames(gorman.vars) <- gorman.vars$title
gorman_dist <- get_dist(gorman.vars |> dplyr::select(-title), method = "spearman")
mat <- as.matrix(gorman_dist)
d_long <- expand.grid(Var1 = rownames(mat), Var2 = colnames(mat))
d_long$value <- as.vector(mat)
d_long$value <- round(d_long$value, digits = 2)
d_long$x_axis <- as.integer(d_long$Var1)
d_long$y_axis <- as.integer(d_long$Var2)
d_long <- as.data.frame(d_long)
graph <- fviz_dist(gorman_dist, show_labels = TRUE)
graph <- graph + geom_text(
data = d_long,
aes(x = x_axis, y = y_axis, label = value),
color = "black",
size.unit = "pt",
size = 5,
inherit.aes = FALSE)
graph + scale_fill_gradient2(low = "blue", high = "red", midpoint = mean(unlist(scores.correlation.per.text)))
View(d_long)
gorman_dist
mat
graph <- fviz_dist(gorman_dist, show_labels = TRUE)
graph
?fviz_dist
graph <- fviz_dist(gorman_dist, show_labels = FALSE) + scale_x_continuous(breaks = 30, labels = colnames(gorman_dist))
graph
?scale_x_discrete
graph <- fviz_dist(gorman_dist, show_labels = FALSE) + scale_x_discrete(labels = colnames(gorman_dist))
graph
?scale_x_discrete
graph <- fviz_dist(gorman_dist, show_labels = FALSE) + scale_x_continuous(breaks = 1:30, labels = colnames(gorman_dist))
graph
?scale_x_continuous
scales::transform_reciprocal()
length(colnames(gorman_dist))
colnames(gorman_dist)
gorman_dist
gorman_dist |> type
gorman_dist |> typeof()
attributes(gorman_dist)$names |> typeof()
attributes(gorman_dist)$name |> typeof()
attributes(gorman_dist)$dim |> typeof()
attributes(gorman_dist)
graph <- fviz_dist(gorman_dist, show_labels = FALSE) + scale_x_continuous(breaks = 1:30, labels = attributes(gorman_dist)$Labels)
graph
attributes(gorman_dist)$Labels
graph <- fviz_dist(gorman_dist, show_labels = FALSE) + scale_x_continuous(breaks = 1:30, labels = attributes(gorman_dist)$Labels)
graph
rlang::last_trace()
graph <- fviz_dist(gorman_dist, show_labels = FALSE) + scale_x_discrete(labels = attributes(gorman_dist)$Labels)
graph
?scale_x_discrete
graph <- fviz_dist(gorman_dist, show_labels = TRUE) + scale_x_discrete(labels = attributes(gorman_dist)$Labels)
graph <- graph + geom_text(
data = d_long,
aes(x = x_axis, y = y_axis, label = value),
color = "black",
size.unit = "pt",
size = 5,
inherit.aes = FALSE)
graph <- fviz_dist(gorman_dist, show_labels = TRUE) + scale_x_discrete(labels = attributes(gorman_dist)$Labels)
graph
graph <- graph + geom_text(
data = d_long,
aes(x = x_axis, y = y_axis, label = value),
color = "black",
size.unit = "pt",
size = 5,
inherit.aes = FALSE)
graph + scale_fill_gradient2(low = "blue", high = "red", midpoint = mean(unlist(scores.correlation.per.text)))
ggsave("Images/dissimilarity_matrix_between_books.png", dpi = "print")
gorman_dist
View(gorman_dist)
View(as.data.frame(gorman_dist))
View(as.matrix(gorman_dist))
d_long
graph <- fviz_dist(gorman_dist, show_labels = TRUE) + scale_x_discrete(labels = attributes(gorman_dist)$Labels) + scale_y_discrete(labels = attributes(gorman_dist)$Labels)
graph <- graph + geom_text(
data = d_long,
aes(x = x_axis, y = y_axis, label = value),
color = "black",
size.unit = "pt",
size = 5,
inherit.aes = FALSE)
graph + scale_fill_gradient2(low = "blue", high = "red", midpoint = mean(unlist(scores.correlation.per.text)))
ggsave("Images/dissimilarity_matrix_between_books.png", dpi = "print")
graph + scale_fill_gradient2(low = "red", high = "blue", midpoint = 0.1)
ggsave("Images/dissimilarity_matrix_between_books.png", dpi = "print")
d_long
d_long <- expand.grid(Var1 = as.factor(rownames(mat)), Var2 = as.factor(colnames(mat)))
d_long$value <- as.vector(mat)
d_long$value <- round(d_long$value, digits = 2)
d_long$x_axis <- as.integer(d_long$Var1)
d_long$y_axis <- as.integer(d_long$Var2)
d_long <- as.data.frame(d_long)
graph <- fviz_dist(gorman_dist, show_labels = TRUE) + scale_x_discrete(labels = attributes(gorman_dist)$Labels) + scale_y_discrete(labels = attributes(gorman_dist)$Labels)
graph <- graph + geom_text(
data = d_long,
aes(x = x_axis, y = y_axis, label = value),
color = "black",
size.unit = "pt",
size = 5,
inherit.aes = FALSE)
graph + scale_fill_gradient2(low = "blue", high = "red", midpoint = mean(unlist(scores.correlation.per.text)))
graph + scale_fill_gradient2(low = "red", high = "blue", midpoint = 0.1)
graph + scale_fill_gradient2(low = "red", high = "blue", midpoint = 0.1)
ggsave("Images/dissimilarity_matrix_between_books.png", dpi = "print")
d_long
?rep
rep(1:30, times = 30)
rep(1:length(gorman.vars$title), each = length(gorman.vars$title))
d_long$x_axis <- rep(1:length(gorman.vars$title), times = length(gorman.vars$title))
d_long$y_axis <- rep(1:length(gorman.vars$title), each = length(gorman.vars$title))
graph <- fviz_dist(gorman_dist, show_labels = TRUE) + scale_x_discrete(labels = attributes(gorman_dist)$Labels) + scale_y_discrete(labels = attributes(gorman_dist)$Labels)
graph <- graph + geom_text(
data = d_long,
aes(x = x_axis, y = y_axis, label = value),
color = "black",
size.unit = "pt",
size = 5,
inherit.aes = FALSE)
graph + scale_fill_gradient2(low = "blue", high = "red", midpoint = mean(unlist(scores.correlation.per.text)))
ggsave("Images/dissimilarity_matrix_between_books.png", dpi = "print")
graph + scale_fill_gradient2(low = "red", high = "blue", midpoint = 0.1)
graph + scale_fill_gradient2(low = "red", high = "blue", midpoint = 0.1)
ggsave("Images/dissimilarity_matrix_between_books.png", dpi = "print")
?scale_fill_gradient
mat
graph <- fviz_dist(mat, show_labels = TRUE) + scale_x_discrete(labels = attributes(gorman_dist)$Labels) + scale_y_discrete(labels = attributes(gorman_dist)$Labels)
?scale_fill_gradient
gorman_dist$Label
attributes(gorman_dist)$Label
#gorman_dist <- proxy::dist(gorman.vars |> dplyr::select(-title), method = "cosine")
#gorman_dist <- get_dist(gorman.vars |> dplyr::select(-title), method = "euclidean")
attributes(gorman_dist)$Label <- as.factor(attributes(gorman_dist)$Label)
mat <- as.matrix(gorman_dist)
d_long <- expand.grid(Var1 = as.factor(rownames(mat)), Var2 = as.factor(colnames(mat)))
d_long$value <- as.vector(mat)
d_long$value <- round(d_long$value, digits = 2)
d_long$x_axis <- rep(1:length(gorman.vars$title), times = length(gorman.vars$title))
d_long$y_axis <- rep(1:length(gorman.vars$title), each = length(gorman.vars$title))
d_long <- as.data.frame(d_long)
graph <- fviz_dist(gorman_dist, show_labels = TRUE) #+ scale_x_discrete(labels = attributes(gorman_dist)$Labels) + scale_y_discrete(labels = attributes(gorman_dist)$Labels)
graph
typeof(names(mat))
mat
attributes(mat)
attribues(gorman_dist)$dimnames[[1]] <- as.factor(attribues(gorman_dist)$dimnames[[1]])
attributes(gorman_dist)$dimnames[[1]] <- as.factor(attribues(gorman_dist)$dimnames[[1]])
attributes(gorman_dist)$dimnames[[1]] <- as.factor(attributes(gorman_dist)$dimnames[[1]])
?attributes
?attr
attributes(gorman_dist)
gorman_dist <- get_dist(gorman.vars |> dplyr::select(-title), method = "spearman")
#gorman_dist <- proxy::dist(gorman.vars |> dplyr::select(-title), method = "cosine")
#gorman_dist <- get_dist(gorman.vars |> dplyr::select(-title), method = "euclidean")
attributes(gorman_dist)$Label <- as.factor(attributes(gorman_dist)$Label)
attributes(gorman_dist)$Labels <- as.factor(attributes(gorman_dist)$Labels)
graph <- fviz_dist(gorman_dist, show_labels = TRUE) #+ scale_x_discrete(labels = attributes(gorman_dist)$Labels) + scale_y_discrete(labels = attributes(gorman_dist)$Labels)
graph
fviz_dist
?fviz_dist
fviz_dist <- function (dist.obj, order = TRUE, show_labels = TRUE, lab_size = NULL,
gradient = list(low = "red", mid = "white", high = "blue"))
{
if (!inherits(dist.obj, "dist"))
stop("An object of class dist is required.")
if (order) {
res.hc <- stats::hclust(dist.obj, method = "ward.D2")
dist.obj <- as.matrix(dist.obj)[res.hc$order, res.hc$order]
}
else dist.obj <- as.matrix(dist.obj)
rownames(dist.obj) <- colnames(dist.obj) <- paste0(rownames(dist.obj),
"-")
d <- data.frame(Var1 = rep(rownames(dist.obj), ncol(dist.obj)),
Var2 = rep(colnames(dist.obj), each = nrow(dist.obj)),
value = as.vector(dist.obj))
d$Var1 <- as.factor(d$Var1)
d$Var2 <- as.factor(d$Var2)
p <- ggplot(d, aes(x = .data[["Var1"]], y = .data[["Var2"]])) +
ggplot2::geom_tile(aes(fill = .data[["value"]]))
if (is.null(gradient$mid))
p <- p + ggplot2::scale_fill_gradient(low = gradient$low,
high = gradient$high)
else p <- p + ggplot2::scale_fill_gradient2(midpoint = mean(dist.obj),
low = gradient$low, mid = gradient$mid, high = gradient$high,
space = "Lab")
if (show_labels)
p <- p + theme(axis.title.x = element_blank(), axis.title.y = element_blank(),
axis.text.x = element_text(angle = 45, hjust = 1,
size = lab_size), axis.text.y = element_text(size = lab_size))
else p <- p + theme(axis.text = element_blank(), axis.ticks = element_blank(),
axis.title.x = element_blank(), axis.title.y = element_blank())
return(p)
}
graph <- fviz_dist(gorman_dist, show_labels = TRUE) #+ scale_x_discrete(labels = attributes(gorman_dist)$Labels) + scale_y_discrete(labels = attributes(gorman_dist)$Labels)
graph
fviz_dist
rm(fviz_dist)
graph <- graph +
geom_text(
data = d_long,
aes(x = x_axis, y = y_axis, label = value),
color = "black",
size.unit = "pt",
size = 5,
inherit.aes = FALSE) +
geom_tile(fill = value)
?geom_tile
View(fviz_dist)
graph <- graph +
geom_text(
data = d_long,
aes(x = x_axis, y = y_axis, label = value),
color = "black",
size.unit = "pt",
size = 5,
inherit.aes = FALSE) +
geom_tile(aes(fill = value), inherit.aes = TRUE)
graph
graph <- fviz_dist(gorman_dist, show_labels = TRUE) + scale_x_discrete(labels = attributes(gorman_dist)$Labels) + scale_y_discrete(labels = attributes(gorman_dist)$Labels)
graph <- graph +
geom_text(
data = d_long,
aes(x = x_axis, y = y_axis, label = value),
color = "black",
size.unit = "pt",
size = 5,
inherit.aes = FALSE) +
geom_tile(aes(fill = value), inherit.aes = TRUE)
graph + scale_fill_gradient2(low = "blue", high = "red", midpoint = mean(unlist(scores.correlation.per.text)))
ggsave("Images/dissimilarity_matrix_between_books.png", dpi = "print")
ggsave("Images/dissimilarity_matrix_between_books.png", dpi = "print")
graph + scale_fill_gradient2(low = "red", high = "blue", midpoint = 0.1)
ggsave("Images/dissimilarity_matrix_between_books.png", dpi = "print")
graph + scale_fill_gradient2(low = "red", high = "blue", midpoint = 0.1)
ggsave("Images/dissimilarity_matrix_between_books.png", dpi = "print")
graph <- graph +
geom_tile(
data = d_long,
aes(fill = value),
inherit.aes = FALSE
) +
geom_text(
aes(x = x_axis, y = y_axis, label = value),
color = "black",
size.unit = "pt",
size = 5,
inherit.aes = TRUE)
graph
d_long <- expand.grid(Var1 = as.factor(rownames(mat)), Var2 = as.factor(colnames(mat)))
d_long$value <- as.vector(mat)
d_long$value <- round(d_long$value, digits = 2)
d_long$x_axis <- rep(1:length(gorman.vars$title), times = length(gorman.vars$title))
d_long$y_axis <- rep(1:length(gorman.vars$title), each = length(gorman.vars$title))
d_long <- as.data.frame(d_long)
graph <- graph +
geom_tile(
data = d_long,
aes(fill = value),
inherit.aes = FALSE
) +
geom_text(
data = d_long,
aes(x = x_axis, y = y_axis, label = value),
color = "black",
size.unit = "pt",
size = 5,
inherit.aes = TRUE)
graph
head(d_long)
graph <- graph +
geom_tile(
data = d_long,
aes(fill = value),
inherit.aes = FALSE
) +
geom_text(
data = d_long,
aes(x = x_axis, y = y_axis, label = value),
color = "black",
size.unit = "pt",
size = 5,
inherit.aes = FALSE)
graph <- fviz_dist(gorman_dist, show_labels = TRUE) + scale_x_discrete(labels = attributes(gorman_dist)$Labels) + scale_y_discrete(labels = attributes(gorman_dist)$Labels)
graph <- graph +
geom_tile(
data = d_long,
aes(fill = value),
inherit.aes = FALSE
) +
geom_text(
data = d_long,
aes(x = x_axis, y = y_axis, label = value),
color = "black",
size.unit = "pt",
size = 5,
inherit.aes = FALSE)
GRAPH
graph
graph <- graph +
geom_tile(
data = d_long,
aes(x = x_axis, y = y_axis, fill = value),
inherit.aes = FALSE
) +
geom_text(
data = d_long,
aes(x = x_axis, y = y_axis, label = value),
color = "black",
size.unit = "pt",
size = 5,
inherit.aes = FALSE)
graph <- fviz_dist(gorman_dist, show_labels = TRUE) + scale_x_discrete(labels = attributes(gorman_dist)$Labels) + scale_y_discrete(labels = attributes(gorman_dist)$Labels)
graph <- graph +
geom_tile(
data = d_long,
aes(x = x_axis, y = y_axis, fill = value),
inherit.aes = FALSE
) +
geom_text(
data = d_long,
aes(x = x_axis, y = y_axis, label = value),
color = "black",
size.unit = "pt",
size = 5,
inherit.aes = FALSE)
graph
ggsave("Images/dissimilarity_matrix_between_books.png", dpi = "print")
graph + scale_fill_gradient2(low = "red", high = "blue", midpoint = 0.1)
?scale_fill_gradient
graph <- fviz_dist(gorman_dist, show_labels = TRUE) + scale_x_discrete(labels = attributes(gorman_dist)$Labels) + scale_y_discrete(labels = attributes(gorman_dist)$Labels)
graph
graph <- graph +
geom_tile(
data = d_long,
aes(x = x_axis, y = y_axis, fill = value),
inherit.aes = FALSE
) +
geom_text(
data = d_long,
aes(x = x_axis, y = y_axis, label = value),
color = "black",
size.unit = "pt",
size = 5,
inherit.aes = FALSE)
graph
?geom_tile
graph + scale_fill_gradient2(low = "blue", high = "red", midpoint = mean(unlist(scores.correlation.per.text)))
ggsave("Images/dissimilarity_matrix_between_books.png", dpi = "print")
graph + scale_fill_gradient2(low = "red", high = "blue", midpoint = 0.1)
ggsave("Images/dissimilarity_matrix_between_books.png", dpi = "print")
mat
gorman_dist
View(gorman_dist |> rowwise() |> mutate(means = across(everything(), mean))
)
library(tidyverse)
)
View(gorman_dist |> rowwise() |> mutate(means = across(everything(), mean)))
View(as_tibble(mat) |> rowwise() |> mutate(means = across(everything(), mean)))
View(as_tibble(mat) |> rowwise() |> mutate(means = c_across(everything(), mean)))
View(as_tibble(mat) |> rowwise() |> mutate(means = mean(1:30)))
?c_across
View(as_tibble(mat) |> rowwise() |> mutate(means = mean(c_across(1:30))))
mean(mat[1,])
mean(mat[2,])
mat[17:30,17:30]
View(as_tibble(mat[17:30,17:30]) |> rowwise() |> mutate(means = mean(c_across(1:30))))
View(as_tibble(mat[17:30,17:30]) |> rowwise() |> mutate(means = mean(c_across(1:17))))
View(as_tibble(mat[17:30,17:30]) |> rowwise() |> mutate(means = mean(c_across(1:13))))
knitr::opts_chunk$set(echo = TRUE)
knitr::opts_knit$set(root.dir = '~/PycharmProjects/corpus-caesarianum-authorship')
rownames(caesar_counts) <- colnames(caesar_counts)
#These indices currently only select Caesar
caesar_counts <- as_tibble(mat[17:30,17:30]) |> rowwise() |> mutate(means = mean(c_across(1:13)))
rownames(caesar_counts) <- colnames(caesar_counts)
View(caesar_counts)
dim(caesar_counts)
caesar_counts$title <- colnames(caesar_counts)[-15]
View(caesar_counts)
gallic_counts <- as_tibble(mat[22:29,12:29]) |> rowwise() |> mutate(means = mean(c_across(1:8)))
View(final.frame)
View(gallic_counts)
gallic_counts <- as_tibble(mat[22:29,22:29]) |> rowwise() |> mutate(means = mean(c_across(1:8)))
caesar_counts$title <- colnames(caesar_counts)[-9]
gallic_counts$title <- colnames(gallic_counts)[-9]
